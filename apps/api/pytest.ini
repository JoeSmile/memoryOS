[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    integration: optional tests that require external services (e.g. HARNESS_TCP_URL)
